import * as React from 'react';
import styles from '../style/main.scss';

export function App() {
    return(
        <header>
            <h1 className={styles.example}>mentiRu</h1>
        </header>
    );
}
